# Production environment — Namecheap VPS
# Copy to: /var/www/uniqueskyway/shared/.env.production
# chmod 600 /var/www/uniqueskyway/shared/.env.production

NODE_ENV=production
PORT=3000
HOSTNAME=127.0.0.1

NEXT_PUBLIC_APP_URL=https://uniqueskyway.com
NEXT_PUBLIC_APP_NAME=Unique Sky Way

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

RESEND_API_KEY=
EMAIL_FROM=Unique Sky Way <info@uniqueskyway.com>

CRON_SECRET=
MAINTENANCE_MODE=false

# Optional pre-launch privacy gate (remove when fully public)
# SITE_ACCESS_KEY=

# Optional override for registration flag
# REGISTRATIONS_ENABLED=true

# Set at deploy time (optional, for /api/health diagnostics)
GIT_COMMIT_SHA=
GIT_COMMIT_REF=
DEPLOYMENT_ID=
